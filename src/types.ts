type BlogPreview = {
    id: string,
    title: string,
    description: string,
    timestamp: Date
}

type Blog = {
    id: string,
    title: string,
    content: string,
    timestamp: Date
}